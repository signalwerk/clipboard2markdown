# Paste to Markdown

Converts HTML to Markdown. Paste anywhere on the page or enter HTML manually.

Uses [Turndown](https://github.com/mixmark-io/turndown) for conversion and [Prettier](https://prettier.io/) for formatting.

## Usage

```bash
npm install
npm run dev
```
